language: c++

os:
  - linux
  - osx

install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; brew install qt
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; sudo apt-get -y install qt5-default 

script:
  - qmake
  - make
